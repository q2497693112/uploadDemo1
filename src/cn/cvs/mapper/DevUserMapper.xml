<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.cvs.mapper.DevUserMapper">

    <select id="LoginDev" resultType="DevUser">select*from dev_user where devCode=#{devCode} and devPassword=#{devPassword}</select>
</mapper>